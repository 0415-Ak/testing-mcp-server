[project]
name = "mcp-on-cloud"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "asyncpg>=0.30.0",
    "fastmcp>=3.4.4",
    "python-dotenv>=1.0.1",
]
